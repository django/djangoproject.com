name: prek checks

on:
    pull_request:
    push:
        branches: [main]

jobs:
    prek:
        runs-on: ubuntu-24.04
        steps:
            - uses: actions/checkout@v6
            - uses: j178/prek-action@v1.1.1
